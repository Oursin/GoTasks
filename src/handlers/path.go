package handlers

const (
	//HTMLPath variable defines the default path for html files
	HTMLPath = "/site/views/"
	//TemplatesPath variable defines the default path for templates
	TemplatesPath = HTMLPath + "templates/"
	//ImagesPath variable defines the default path for images files
	ImagesPath = "/site/static/images/"
)
